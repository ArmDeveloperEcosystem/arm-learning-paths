{
    "name": "Arm Learning Paths",

    "image": "armswdev/learn-dev-container"

    "forwardPorts": [1313],

    "remoteUser": "ubuntu",

    "postStartCommand": "nohup bash -c 'hugo server --baseUrl=/ --liveReloadPort=443 --appendPort=false --bind=0.0.0.0 &' "

}
